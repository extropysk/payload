{
  "name": "payload",
  "private": true,
  "workspaces:": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "engines": {
    "node": ">=14",
    "pnpm": ">=9.7.0"
  },
  "packageManager": "pnpm@9.7.0",
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}
